# 西红柿全流程测试报告（二）：接入真实 AI 后的对比

测试时间：2026/06/26 ｜ 供应商：云雾API（OpenAI 兼容中转）｜ 接入方式：providerConfig（仅本次测试）

---

## 〇、模型说明（重要）

- 你指定的 `gpt-5.5-pro` **在云雾不存在**，调用直接返回 `429 分组 openai 没有可用模型`。
- 云雾该 Key 实际可用 461 个模型，5.5 系列只有 `gpt-5.5` 和 `gpt-5.5-2026-04-24`，无 `-pro` 变体。
- 本次实测用最贴近的 **`gpt-5.5`**（旗舰、支持视觉）。如需 pro 档，云雾有 `gpt-5.4-pro-2026-03-05` / `gpt-5.2-pro` 可换。
- 测试图：从公开图库下载的一张真实西红柿照片（4 颗带藤、白底产品图）。

---

## 一、结论速览

| 维度 | 结论 |
|---|---|
| **流程是否跑通** | ✅ 5 步全部 HTTP 200，无报错 |
| **运行耗时** | **445 秒 ≈ 7.4 分钟**（mock 模式仅 ~0.9s） |
| **识图是否真生效** | ✅ **质的飞跃**，真正"看到"了西红柿 |
| **文案是否变好（核心）** | ❌ **几乎没变**。14 屏标题里 12 屏与 mock 逐字相同 |

**一句话**：接真实 AI 让"识图/市场/风格/提示词"4 步真实生效，但**最关键的文案这一步几乎没受益**——因为代码在 AI 之后用硬编码模板把 AI 写的标题/正文**覆盖丢弃**了。上一份报告的代码推断，这次被实测 100% 证实。

---

## 二、各步耗时（真实 AI）

| 步骤 | 接口 | 状态 | 耗时 | 备注 |
|---|---|---|---|---|
| 1 识图 | analyze-product | ✅ | 32.7s | gpt-5.5 视觉 |
| 2 市场验证 | research-product | ✅ | 121.2s | ⚠️ 逼近 120s 客户端超时上限 |
| 3 视觉风格 | generate-visual-style | ✅ | 49.9s | |
| 4 策划方案 | generate-design-plan | ✅ | 120.6s | ⚠️ 逼近超时上限 |
| 5 图像提示词 | generate-prompts | ✅ | 120.6s | ⚠️ 逼近超时上限 |
| **合计** | | | **445.1s** | |

> 注意：步骤 2/4/5 都卡在 ~120s，正好是 `openai-client` 里 120000ms 的超时上限。gpt-5.5 较慢，**生产环境有触发超时失败的风险**，建议调高超时或换更快的模型（如 gpt-4o / gpt-5-mini）。

---

## 三、识图（步骤1）— 真实 AI 完全生效 ✅

| 字段 | mock 模式（错） | 真实 AI（对） |
|---|---|---|
| 品类 | 新鲜蔬菜/番茄（靠手填覆盖） | 食品 / 生鲜果蔬 / 番茄 |
| 名称 | 沙瓤普罗旺斯西红柿（手填） | 番茄（西红柿） |
| **外观** | 白色机身/水箱/格栅/脚轮（**风扇**） | 4个成熟番茄、带绿色果蒂与枝梗、白底摆拍 |
| **材质** | 塑料机身/透明水位窗 | 番茄果皮高光质感、植物纤维枝梗 |
| **颜色** | 白色/深灰/浅灰 | 红色/绿色/白色 |

识图从"瞎编风扇"变成"准确描述西红柿"，这一步真实 AI 的价值非常明确。市场验证、视觉风格、提示词同理（都基于准确识图，质量提升）。

---

## 四、策划文案（步骤4）— 真实 AI 几乎无效 ❌（核心问题）

### 14 屏详情页主标题对比

| 屏 | mock | 真实 AI | |
|---|---|---|---|
| P1–P6 | 判断更快 | 判断更快 | 相同 |
| P7 | 少一次搬挪和重新摆放 | 判断更快 | ★不同（**真实AI反而更差**） |
| P8 | 判断更快 | 判断更快 | 相同 |
| P9 | 少一点频繁补充或中断 | 判断更快 | ★不同（**真实AI反而更差**） |
| P10 | 判断更快 | 判断更快 | 相同 |
| P11 | 结构看清楚 | 结构看清楚 | 相同 |
| P12 | 判断更快 | 判断更快 | 相同 |
| P13 | 少一点反复判断的麻烦 | 少一点反复判断的麻烦 | 相同 |
| P14 | 安心一点 | 安心一点 | 相同 |

**12/14 逐字相同**，仅有的 2 屏不同还是真实 AI 版**退化**了。

### 主图副标题/正文：连"错误"都一模一样

真实 AI 版主图1（原文照抄）：
- subheadline：`使用场景里，少点注重健康饮食的家庭会带来` ← 与 mock **逐字相同**的残缺句
- body：`...客厅/卧室/厨房等家庭日更好判...` ← **西红柿页面仍出现客厅/卧室**

> 关键点：mock 模式那次，"客厅/卧室/厨房"还能甩锅给底层风扇假数据；**这次底层已经是准确的西红柿了，"客厅/卧室"照样出现**——证明它根本不是数据问题，而是 `audienceSceneFromText` 里"家庭→客厅/卧室/厨房"的**硬编码**，与产品、与 AI 全都无关。

唯一可见差别：个别 body 落款从"来自当前资料"变成"来自图片信息"，主图5 漏出一句真实卖点"酸甜可生吃"。

---

## 五、为什么接 AI 修不了文案（实测+代码双重确认）

`generate-design-plan.ts` 调用 AI 后的处理链：

```
AI 生成方案 → parseDesignPlanJson()
  → completeDesignPlan() → applyManualInfoToDesignPlan()
  → applyCreativeRulesToDesignPlan()  ← 问题在这
  → finalizeDesignPlan()
```

`applyCreativeRulesToDesignPlan` 里（generate-design-plan.ts:2147 / 2243）：

```js
copywriting: {
  ...item.copywriting,              // AI 写的标题/正文铺进来
  headline:    guardedMainCopy.headline,     // ← 立刻被模板覆盖
  subheadline: guardedMainCopy.subheadline,  // ← 覆盖
  body:        guardedMainCopy.body          // ← 覆盖
}
```

`guardedMainCopy` 来自硬编码的 `headlineFromPoint / subheadlineFromPoint / fabCopyForMain`。
**实测证明**：gpt-5.5 跑了 120 秒，产出的 headline/subheadline/body 被整段丢弃，最终展示的就是这套模板的输出——所以和 mock 逐字相同。

---

## 六、三个根因（与上一份报告一致，已被实测确认仍然存在）

| # | 现象 | 根因 | 位置 |
|---|---|---|---|
| A | 标题全是"判断更快" | 生鲜/蔬果未进类目识别，掉"通用商品"兜底；且卖点→标题用硬编码关键词映射 | `scene-emotion-map.ts:88-105`、`generate-design-plan.ts` `headlineFromPoint` |
| B | 西红柿页出现"客厅/卧室/厨房" | 人群→场景硬编码（家庭→客厅/卧室/厨房），无视品类与 AI | `ai.ts:111-116` |
| C | 副标题语法残缺、被截断 | 伪痛点 `{人群}会关注页面是否讲清…` + 固定字数 clip 截断 | `ai.ts:204`、`fab-copywriting.ts:152-158` |
| **D（新）** | **AI 文案被丢弃** | 后处理无条件用模板覆盖 AI 的 headline/subheadline/body | `generate-design-plan.ts:2147、2243` |

---

## 七、建议（按性价比排序）

1. **先解决 D：让 AI 文案生效。** 在 `applyCreativeRulesToDesignPlan` 改为"AI 有合法文案就保留，仅在缺失时用模板兜底"——这是把"接了 AI 却没用上"变成"接了就有效"的最小改动，收益最大。
2. **再修 A/B/C** 那套确定性模板（它仍承担兜底职责，质量要补齐）：类目识别加生鲜/蔬果、场景映射结合品类、去掉伪痛点与硬截断。
3. **超时与模型**：2/4/5 步逼近 120s 超时。要么调高 `openai-client` 超时，要么改用更快模型；gpt-5.5 这类慢模型在生产环境有失败风险。
4. **模型名校验**：`gpt-5.5-pro` 不存在却只回笼统的 429。建议接入时先拉一次 `/v1/models` 校验模型名，给用户明确报错。

---

## 八、原始数据
- 真实 AI 各步返回：`/tmp/tomato/real-1-analyze.json` … `real-5-prompts.json`
- mock 各步返回：`/tmp/tomato/1-analyze.json` … `5-prompts.json`
- 测试图：`/tmp/tomato/tomato.jpg`
- 测试脚本：`/tmp/tomato/run-real.mjs`（API Key 已抹除）
